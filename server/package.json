{
  "name": "smart-office-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Thin Express proxy between the browser and the n8n webhooks. Holds N8N_SECRET.",
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env index.js",
    "start": "node --env-file-if-exists=.env index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
